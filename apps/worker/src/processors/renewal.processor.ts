// TODO: not implemented. Should raise renewal invoices and queue reminder emails.
// Returns true only so the module typechecks; it does not process anything.
export function processRenewalJobs() {
  return true;
}
