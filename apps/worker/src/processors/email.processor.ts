// TODO: not implemented. Should render a Handlebars template and send via SES.
// Returns true only so the module typechecks; it does not process anything.
export function processEmailJobs() {
  return true;
}
