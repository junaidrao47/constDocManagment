// TODO: not implemented. Should dump Postgres and upload to S3_BACKUP_BUCKET.
// Returns true only so the module typechecks; no schedule is registered.
export function runBackupCron() {
  return true;
}
