// TODO: not implemented. Should validate uploads and advance document status.
// Returns true only so the module typechecks; it does not process anything.
export function processDocumentJobs() {
  return true;
}
