// TODO: not implemented. Should run nightly and queue renewal jobs for
// subscriptions expiring inside the reminder window.
// Returns true only so the module typechecks; no schedule is registered.
export function runRenewalCron() {
  return true;
}
